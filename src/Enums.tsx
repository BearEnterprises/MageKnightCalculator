export enum Damage {
    Physical,
    Ice,
    Fire,
    ColdFire,
}

export enum Phase {
    Physical,
    Ice,
    Fire,
    ColdFire,
}
export enum Phases {
    RangedSeige,
    Block,
    AssignDamage,
    Attack,
}